<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.mybatis.dao.StudentMapper">

	<!--public List<Employee> getEmps(); -->
	<select id="getEmps" resultType="com.atguigu.mybatis.bean.Student">
		select * from student
	</select>

	<insert id="insert">
		insert into student(id) values(11)
	</insert>
</mapper>